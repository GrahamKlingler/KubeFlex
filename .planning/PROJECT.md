# KubeFlex

## What This Is

KubeFlex is a carbon-aware container live migration system for Kubernetes, being extended with advanced migration heuristics and distributed workload migration for a thesis. The system uses CRIU to checkpoint/restore running containers between KIND cluster nodes, choosing target regions based on forecasted carbon intensity from US power grid data. The next phase of work focuses on smarter migration decisions that account for hardware heterogeneity, job deadlines, checkpoint/restore overhead, and runtime estimates — and on enabling migration of multi-node distributed workloads regardless of their networking library.

## Core Value

Prove that carbon-aware migration heuristics — informed by hardware specs, job deadlines, and migration overhead — reduce total carbon emissions compared to naive scheduling policies, validated through both simulation and cluster experiments.

## Requirements

### Validated

- ✓ Carbon-aware container migration on KIND cluster (3 workers, 3 regions) — existing
- ✓ 5 scheduling policies (placement-only through always-best) — existing
- ✓ CRIU-based single-pod live migration — existing
- ✓ PostgreSQL carbon intensity data (2020-2022, hourly, 14 US regions) — existing
- ✓ Expected-mode simulation (no cluster needed, fast iteration) — existing
- ✓ Test result graphing (matplotlib) — existing
- ✓ Controller/Migration Service/Migrator architecture — existing
- ✓ Forecast-based decision making (policies 3-5) — existing

### Active

- [ ] Implement heuristic from heuristic.txt as new scheduling policy
- [ ] Static hardware lookup table (cores, wattage per region from Nautilus data)
- [ ] Checkpoint/restore/send overhead estimation functions
- [ ] Job runtime estimation
- [ ] Deadline-aware migration decisions
- [ ] Periodic re-evaluation of migration decisions during job execution
- [ ] Heuristic variant exploration (forecast horizons, with/without deadline awareness, etc.)
- [ ] Evaluation framework comparing new heuristic(s) against existing 5 policies
- [ ] Simulation-based evaluation results
- [ ] Real cluster (KIND) evaluation results
- [ ] Network-agnostic distributed checkpointing (proxy-based traffic buffering during checkpoint)
- [ ] Multi-node workload migration (MPI and beyond)

### Out of Scope

- Runtime hardware availability queries (Nautilus API) — static lookup is sufficient for thesis
- Production deployment — this is research/thesis work on local KIND clusters
- New carbon data sources — existing 2020-2022 dataset is sufficient
- GUI or web dashboard — results are graphs generated by existing scripts
- Always-on network proxy — only buffer during checkpoint window

## Context

- Thesis work — no hard deadline, but working toward completion
- Heuristic work is the primary contribution; distributed migration is secondary
- Hardware specs (cores, processors, wattage) sourced from Nautilus Research Project
- Existing simulation mode (`--expected`) enables fast heuristic iteration without a live cluster
- The heuristic in `heuristic.txt` captures the core logic: evaluate migration cost (checkpoint + send + restore carbon) against forecast carbon savings at destination, respecting deadlines
- Evaluation gap is the main challenge: proving the new heuristic outperforms existing policies
- For distributed migration, professor suggested proxy-based approach: buffer network traffic during checkpoint, replay after restore — keeps CRIU as the checkpoint mechanism
- CRIU is preferred for distributed checkpointing due to its versatility (process-level, not application-level)

## Constraints

- **Platform**: CRIU only works on Linux; KIND on macOS can run cluster but not actual CRIU operations
- **Data range**: Carbon data covers 2020-01-01 to 2022-12-31; scheduler-time must fall in this range
- **Cluster topology**: 3 worker nodes hardcoded as kind-worker, kind-worker2, kind-worker3 with regions NE, TEN, CENT
- **Security**: Migration pods require privileged security context (SYS_ADMIN, SYS_PTRACE, CHECKPOINT_RESTORE)
- **Stack**: Python 3.9 in containers, FastAPI, PostgreSQL, APScheduler — maintain consistency with existing codebase

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Static hardware lookup (not runtime query) | Sufficient for thesis evaluation; avoids Nautilus API dependency | — Pending |
| CRIU for distributed checkpointing | Most versatile — works at process level, not application level | — Pending |
| Proxy buffers traffic only during checkpoint | Simpler than always-on proxy; sufficient for correctness | — Pending |
| Periodic re-evaluation of migration decisions | Conditions change during long-running jobs; one-shot decision is suboptimal | — Pending |
| Heuristic is primary thesis contribution | Distributed migration is secondary/future work | — Pending |
| Both simulation and cluster evaluation | Simulation for fast iteration, cluster for validation | — Pending |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-04-17 after initialization*
